
  # Define UI and Navigation

  This is a code bundle for Define UI and Navigation. The original project is available at https://www.figma.com/design/KqumBNOggqIm4joFwGgZCc/Define-UI-and-Navigation.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  